{
  "name": "sih-26125-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "SIH 26125 identity/access/asset Solidity contracts (additive to DPDP healthcare platform). Contracts are authored in Week 2-3; this is the Week 1 scaffold.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "devDependencies": {
    "hardhat": "^2.22.10",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
