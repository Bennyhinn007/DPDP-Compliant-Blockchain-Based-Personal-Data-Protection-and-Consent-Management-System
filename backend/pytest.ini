[pytest]
# Self-contained test suite — runs fully in-memory (mongomock), no MongoDB
# or Ganache required. Just run:  pytest
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q --no-header
